export class Sonda {

    constructor(coordenadas: string, movimentos: string) {
        this.coordenadas = coordenadas;
        this.movimentos = movimentos;
    }

    coordenadas?: string;
    movimentos?: string;
}